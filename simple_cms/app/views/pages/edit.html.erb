<%= link_to "<< Back to List", :back %>

<div class="pages edit">
	<h2>Update Page</h2>

	<%= form_for @page, url: {action: 'update', page_id: @page.id, subject_id: @subject.id} do |f| %>

	  <%= render(partial: 'form', locals: {f: f})  %>
	 
	  <div class="form-buttons">
	    <%= f.submit "Update Page"%>
	  </div>

	<% end %>
</div>
