<%= link_to "<< Back to List", :back %>

<div class="sections edit">
	<h2>Update Section</h2>

	<%= form_for @section, url: {action: 'update', page_id: @page.id} do |f| %>

	  <%= render(partial: 'form', locals: {f: f})  %>
	 
	  <div class="form-buttons">
	    <%= f.submit "Update Section"%>
	  </div>

	<% end %>
</div>